# Week 1 详细学习计划：从 Hello World 到 Agent 初体验

> 时间：2026-05-21（周四）~ 2026-05-25（周一）
> 模块：LLM + Prompt
> 每日投入：2-3 小时

## Day 1（5/21 周四）— LLM 是什么

### 学习内容
- **Handbook 章节**：[大语言模型（LLM）](https://aiweb3.school/zh/handbook/ai/llm/)
- **核心认知**：LLM 不是会聊天的黑盒，而是概率模型。它生成的是"概率上合理的输出"，不是"天然可信的事实"。
- **三个第一性原理**：
  1. 模型是推理层，不是真相源 → 关键事实必须来自数据库/API/文档
  2. 输出要变成可检查对象 → 用 schema、参数、引用承载，不用散文
  3. 不确定性要前移暴露 → 不确定时降级，不编答案

### 知识节点
| 概念 | 一句话理解 |
|------|-----------|
| Token | 模型处理文本的基本单位，不是"一个汉字=一个token" |
| Embedding | 把文本映射成向量，用于语义搜索（不是事实判断） |
| Transformer | 靠 attention 在上下文中找模式，不等于数据库 |
| Hallucination | 编造看起来合理但不真实的内容——不光写 prompt 能解决，要靠外部校验 |
| Multimodal | 能读图/代码截图/UI，但仍需边界判断 |

### 实践任务
1. 用你现在用的 DeepSeek 或其他模型，做 3 个实验：
   - **翻译**：翻一段中英混排的技术文档，观察 token 处理
   - **推理**：问一个逻辑题（如"3 个开关控制 3 盏灯"），看推理路径
   - **局限**：问模型当前日期以外的未来事件，看它是否编造
2. 思考：什么场景下你会信任模型的输出？什么时候你会需要验证？

### 打卡草稿
> Day 1：读完 Handbook LLM 章节。核心收获：LLM 生成的是概率合理的输出，不是事实——这就决定了它在系统里的位置（推理层，不是真相源）。做了三个对比实验，观察了翻译、推理和局限。接下来思考：什么时候信它，什么时候必须验证。

---

## Day 2（5/22 周五）— 深入 LLM：Token、参数与选型

### 学习内容
- **重读**：LLM 章节的 Token、Transformer、Hallucination 部分
- **Token 深入**：
  - Tokenizer 切分规则：中文每个字 1-2 token，英文单词 1-3 token
  - 上下文窗口：GPT-4 128K = 约 9 万英文单词 / 6-8 万中文
  - 成本计算：输入 token × 单价 + 输出 token × 单价
- **关键参数**：
  - Temperature（温度）：0 = 确定性输出，1+ = 创造性/随机
  - Top-P：核采样，和 temperature 配合
  - Max Tokens：限制输出长度，防"废话连篇"
- **模型选型思维**：
  - 推理任务 → Claude / GPT-4 / DeepSeek
  - 简单分类 → 小模型（GPT-4o-mini / Claude Haiku）
  - 代码生成 → 特定模型更好
  - 亚洲语言 → DeepSeek / Qwen

### 实践任务
1. 用一个相同的 prompt（如"用三句话解释区块链"），用不同的 temperature 跑：
   - T=0 → 跑 3 次，看是否完全一致
   - T=0.7 → 跑 3 次，看差别多大
   - T=1.5 → 跑 3 次，观察是否开始"跑偏"
2. 计算一个多轮对话的 token 消耗（估算）

### 打卡草稿
> Day 2：深入 LLM 的 Token 和参数。做了 temperature 对比实验：T=0 稳定，T=0.7 有变化但合理，T=1.5 已经开始发散了。理解了模型选型要看具体任务——不是越大越好。明日进入 Prompt 设计。

---

## Day 3（5/23 周六）— Prompt 基础：把任务写清楚

### 学习内容
- **Handbook 章节**：[提示词（Prompt）](https://aiweb3.school/zh/handbook/ai/prompt/)
- **核心认知**：Prompt 是接口设计，不是魔法咒语。它把模糊任务变成模型可稳定执行的工作说明。
- **Instruction 四段式**：
  1. 任务目标：你要做什么
  2. 可用输入：哪些信息可以使用
  3. 禁止行为：绝对不能做什么
  4. 输出格式：返回什么形态的结果
- **第一性原理**：Prompt 是软约束，不是安全边界。真正的边界由代码、权限、校验和审计承担。

### 知识节点
| 概念 | 一句话理解 |
|------|-----------|
| Instruction | 给模型的任务规则，要区分"解释"和"执行" |
| Few-shot | 放示例让模型模仿格式和判断方式 |
| Structured Output | 让模型返回 JSON / schema，后续系统才能处理 |
| Prompt Injection | 攻击者让模型忽略原始规则——应对方式不是写防注入 prompt |

### 实践任务
1. 写 3 个不同场景的 prompt：
   - **场景 A（交易解释）**：输入一笔交易数据，要求输出资产变化、风险等级、不确定点
   - **场景 B（代码审查）**：输入一段代码，要求列出问题、严重程度、修改建议
   - **场景 C（学习助手）**：输入一个概念，要求用比喻 + 例子 + 边界条件解释
2. 对每个 prompt，测试不同模型输出的差异

### 打卡草稿
> Day 3：读完 Handbook Prompt 章节。核心收获：Prompt 是接口设计，不是咒语——要把任务目标、可用输入、禁止行为和输出格式四段写清楚。写了 3 个场景 prompt（交易解释、代码审查、学习助手），观察到结构化输出比散文式输出更稳定。

---

## Day 4（5/24 周日）— Prompt 进阶：Few-shot、CoT 与 System Prompt

### 学习内容
- **重读**：Prompt 章节的 Few-shot、Structured Output、Prompt Injection 部分
- **Few-shot 精讲**：
  - 好示例 + 坏示例比一堆好示例效果更好
  - 示例是要跟 eval 一起维护的测试资产
- **Chain-of-Thought（CoT）**：
  - 让模型一步步推理，而非直接给答案
  - 适合复杂推理、数学、多步分析任务
- **System Prompt vs User Prompt**：
  - System：固定角色和规则，开发者控制
  - User：每次任务输入，用户控制
  - 不要混在一起，分层管理
- **Structured Output 实践**：
  - 要求 JSON schema：`{"action": "...", "risk_level": "...", "uncertainties": []}`
  - 后续代码可以 check、reject、record

### 实践任务
1. **CoT 实验**：用一个复杂问题（如"如果 Alice 给 Bob 转 1 ETH，Gas 费 0.003 ETH，Alice 余额 2 ETH，Bob 余额 0.5 ETH，交易后各是多少？"），对比直接回答和 CoT（"请一步步分析"）
2. **System Prompt 设计**：写一个 System Prompt，让你成为你的"AI × Web3 学习助手"，规定你的行为边界
3. **结构化输出实验**：让模型用 JSON 格式输出今天的学习总结

### 打卡草稿
> Day 4：完成 Prompt 进阶学习。做了 CoT 实验，发现"一步步分析"确实能减少错误。设计了 System Prompt 来定义自己 AI 助手的边界。结构化输出实验让模型返回 JSON 格式的内容，后续可以自动处理。明天进入本周复习和综合实验。

---

## Day 5（5/25 周一）— 复习 + 综合实验

### 学习内容
- **回顾**：本周 LLM + Prompt 的全部核心概念
- **知识串联**：
  - LLM 是概率模型 → 输出不是事实，需要校验
  - Token 是成本单位 → 长文档和代码需要预算管理
  - Prompt 是接口设计 → 任务目标 + 边界 + 格式
  - 结构化输出 → 机器可检查
  - 软约束 ≠ 安全边界 → 代码层校验不可少

### 实践任务
1. **综合实验**：用 Python 调用一个 LLM API（DeepSeek / OpenAI），实现：
   - 接收用户输入的问题
   - 用一段 System Prompt 定义助手角色
   - 用 CoT 方式回答
   - 返回 JSON 格式的结果（含答案 + 不确定点）
   - 保存到 `experiments/day5-api-call/` 下
2. 写本周总结到 `daily/2026-05-25.md`

### 打卡草稿
> 本周复盘（Week 1）：完成了 AI 基础的前两个模块——LLM 和 Prompt。核心认知：模型生成的是概率输出，不是事实；Prompt 是接口设计，不是魔法。做了 temperature 对比、CoT 推理、结构化输出等实验。下周进入 Context、RAG 和 Agent。最小路径已走通，继续保持节奏。

---

## 本周每日节奏建议

```
20:00-20:10   翻阅当日 Handbook 章节概览
20:10-20:45   精读重点概念 + 做笔记
20:45-21:30   做实践任务 / 实验
21:30-21:45   写 daily note + 整理打卡草稿
21:45-22:00   提交打卡到 WCB 平台
```

## 学习原则

1. **不追求一次读懂** — 概念先有个印象，后面实践会自然理解
2. **每个概念做笔记** — 用自己的话写一遍才算真懂
3. **实验优先** — 别只看 Handbook，动手试一下
4. **打卡不是负担** — 写清楚今天学了什么、卡在哪、明天做什么
